linter:
  rules:
    avoid_print: false  # Uncomment to disable the `avoid_print` rule
    
analyzer: 
  errors: 
    invalid_annotation_target: ignore
  exclude:
    - '**/*.g.dart'
    - '**/*.freezed.dart'